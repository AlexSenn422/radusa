<nav class="navbar navbar-default homepage-nav-bar" id="my-custom-navbar" style="display: none;">
	<div class="container-fluid">
		<!-- Brand and toggle get grouped for better mobile display -->
		<div class="navbar-header mobile">
			<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
				<span class="sr-only">Toggle navigation</span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
			<a class="navbar-brand mobile-logo" href="https://rad-usa.myshopify.com"><img src="https://cdn.shopify.com/s/files/1/0018/2727/4827/files/logo_x96.png?v=1552993674" alt="" /></a>
		</div>

		<!-- Collect the nav links, forms, and other content for toggling -->
		<div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
			<ul class="nav navbar-nav">
              <button class="hamburger hamburger--slider" type="button">
                <span class="hamburger-box">
                  <span class="hamburger-inner"></span>
                </span>
              </button>
              {% for link in linklists.left-menu.links %}
				<li class="menu-item invisible"><a href="{{ link.url }}">{{ link.title }}</a></li>
              {% endfor %}
			</ul>
			<a href="{{shop.url}}" class="site-logo desktop"><img src="{{ 'logoopt.png' | asset_url }}" alt="logo"></a>
			<ul class="nav navbar-nav navbar-right">
              {% for link in linklists.right-menu.links %}
				<li class="menu-item invisible"><a href="{{ link.url }}">{{ link.title }}</a></li>
              {% endfor %}
				<li class="item-count">
					<a href="/cart" class="" id="cartButton"><i class="fa fa-shopping-cart" aria-hidden="true"></i> <span id="cartCount" class="cart-count">{{ cart.item_count }}</span></a>
				</li>
              
              	<li class="currency-selector-item">
                  
                    {% include 'currency-picker' %}
                  
             	</li>
			</ul>			
		</div><!-- /.navbar-collapse -->
		<div class="mobile-cart-icon visible-xs">
			<a href="/cart"><i class="fa fa-shopping-cart" aria-hidden="true"></i><span id="cartCount" class="cart-count">{{ cart.item_count }}</span></a>
		</div>
      
      	<div class="mobile-currency-picker visible-xs">
      		<a class="" href="javascript:void(0);">
              {% include 'currency-picker' %}
            </a>
        </div>
	</div><!-- /.container-fluid -->
</nav>